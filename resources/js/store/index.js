import { createStore } from 'vuex'
import styles from './modules/styles.js'

export default createStore({
    state: () => ({}),
    mutations: {},
    actions: {},
    getters: {},
    modules: {
        styles,
    }
})
